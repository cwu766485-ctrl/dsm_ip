# Observer Bridge Reference Contract

The reference is the transaction contract implemented in the testbench:
ordered I/Q samples, preserved `tlast` and `tuser`, bounded source stall, and
zero dropped samples in strict-backpressure mode.
