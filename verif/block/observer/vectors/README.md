# Observer Bridge Vector Contract

The async bridge testbench creates its deterministic 24-sample transaction
sequence internally. It intentionally tests clock-domain transport and
backpressure without an external generated file.
